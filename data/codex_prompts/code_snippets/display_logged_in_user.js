      // I removed this from the dashboard because I'm the only person using the app.
      
      <Typography variant="h5" sx={{ mb: 1 }}>
        Welcome to your Memory Core, {session?.user?.name || 'User'}!
      </Typography>